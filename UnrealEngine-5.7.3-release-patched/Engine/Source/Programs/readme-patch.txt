UnrealBuildAccelerator 是一个未开发完成的模块，但是必须编译错误才可以正常编译，因为编译错误才能使得编译必须的其他UBA模块正常工作

UnrealBuildAccelerator is an unfinished module, but it must produce compilation errors in order to compile correctly, because the compilation errors are necessary for other UBA modules required for compilation to work properly.

UnrealBuildAccelerator.zip 是一个补丁模块，只简单去除了编译错误的问题，但不能让UBACORE正常工作

UnrealMultiUserSlateServer.zip 可能是由于目录名过长，而导致可能修正错误的模块